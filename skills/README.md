# Skills – kickstart-next-ssg

Each folder has a `SKILL.md` with YAML frontmatter (`name`, `description`). Which skill to open for a task is listed in repo root `AGENTS.md`.

## Skill folders

- `dev-workflow/`
- `contentstack-kickstart/`
- `typescript/`
- `testing/`
- `code-review/`
- `framework/`
